const API_URL='http://localhost:8080/api';function getToken(){return localStorage.getItem('token');}function showToast(m,t='success'){const e=document.getElementById('toast');e.textContent=m;e.className=`toast ${t} show`;setTimeout(()=>e.classList.remove('show'),3000);}if(!getToken())window.location.href='login.html';async function loadTickets(){try{const r=await fetch(`${API_URL}/booking/user`,{headers:{'Authorization':`Bearer ${getToken()}`}});const d=await r.json();const c=document.getElementById('ticketsContainer');if(r.ok&&d.data&&d.data.length>0){c.innerHTML=d.data.map(b=>`<div class="ticket-card"><div class="ticket-head"><div class="ticket-id">${b.bookingId}</div><div class="status-badge status-${b.bookingStatus.toLowerCase()}">${b.bookingStatus}</div></div><div class="detail-row"><span class="detail-label">Bus:</span><span class="detail-value">${b.busNo}</span></div><div class="detail-row"><span class="detail-label">Route:</span><span class="detail-value">${b.source} → ${b.destination}</span></div><div class="detail-row"><span class="detail-label">Date:</span><span class="detail-value">${b.travelDate}</span></div><div class="detail-row"><span class="detail-label">Amount:</span><span class="detail-value" style="color:var(--primary-green);">₹${b.totalAmount}</span></div></div>`).join('');}else{c.innerHTML='<div style="text-align:center;padding:60px 20px;"><h3 style="font-size:1.25rem;font-weight:700;margin-bottom:8px;">No Tickets Yet</h3><p style="color:var(--text-gray);">Start booking your journeys</p></div>';}}catch(e){showToast('Failed to load','error');}}loadTickets();
