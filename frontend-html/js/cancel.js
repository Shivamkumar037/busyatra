const API_URL='http://localhost:8080/api';function getToken(){return localStorage.getItem('token');}function showToast(m,t='success'){const e=document.getElementById('toast');e.textContent=m;e.className=`toast ${t} show`;setTimeout(()=>e.classList.remove('show'),3000);}if(!getToken())window.location.href='login.html';document.getElementById('cancelForm').addEventListener('submit',async(e)=>{e.preventDefault();if(!confirm('Sure to cancel?'))return;const b=e.target.querySelector('button');b.innerHTML='<span class="loading"></span>';try{const r=await fetch(`${API_URL}/ticket/cancel`,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${getToken()}`},body:JSON.stringify({bookingId:document.getElementById('bookingId').value,reason:document.getElementById('reason').value})});const d=await r.json();if(r.ok){showToast('Cancelled! Refund processing.','success');setTimeout(()=>window.location.href='tickets.html',2000);}else{showToast(d.message||'Failed','error');b.innerHTML='<span>Cancel Ticket</span>';}}catch(e){showToast('Network error','error');b.innerHTML='<span>Cancel Ticket</span>';}});
